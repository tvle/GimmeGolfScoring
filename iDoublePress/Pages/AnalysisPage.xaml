<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:iDoublePress.PageModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             xmlns:ext="clr-namespace:iDoublePress.Extensions"
             x:Class="iDoublePress.Pages.AnalysisPage"
             x:DataType="pageModels:AnalysisPageModel"
             x:Name="AnalysisPageView"
             Title="{Binding Title}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            BindingContext="{Binding Path=BindingContext, Source={x:Reference AnalysisPageView}, x:DataType=ContentPage}"
            EventName="NavigatedTo"
            Command="{Binding NavigatedToCommand}" />
    </ContentPage.Behaviors>

    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="{StaticResource LayoutPadding}" Spacing="25">

                <Grid ColumnDefinitions="*,Auto" HorizontalOptions="Fill">
                    <Label Text="{ext:Translate StrokesGained}" Style="{StaticResource Title2}" VerticalOptions="Center"/>
                    <Picker Grid.Column="1"
                            ItemsSource="{Binding Years}"
                            SelectedItem="{Binding SelectedYear}"
                            WidthRequest="120"
                            HorizontalOptions="End" />
                </Grid>

                <VerticalStackLayout Spacing="5">
                    <Label Text="{ext:Translate StrokesGainedDescription}" FontSize="12" TextColor="{StaticResource Gray400}" />
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:CartesianChart
                            Series="{Binding StrokesGainedSeries}"
                            XAxes="{Binding SGAxesX}"
                            YAxes="{Binding SGAxesY}"
                            HeightRequest="220"
                            TooltipPosition="Top">
                        </lvc:CartesianChart>
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="{ext:Translate DrivingPerformance}" Style="{StaticResource Title2}" />

                    <Border StrokeShape="RoundRectangle 10" Padding="15" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <Grid ColumnDefinitions="*,*">
                            <VerticalStackLayout Grid.Column="0" HorizontalOptions="Center">
                                <Label Text="{ext:Translate AverageDrive}" FontSize="12" TextColor="{StaticResource Gray400}"/>
                                <Label Text="{Binding AverageDriveDisplay}" FontSize="24" FontAttributes="Bold" TextColor="{StaticResource Primary}"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                                <Label Text="{ext:Translate LongestDrive}" FontSize="12" TextColor="{StaticResource Gray400}"/>
                                <Label Text="{Binding LongestDriveDisplay}" FontSize="24" FontAttributes="Bold" TextColor="{StaticResource Primary}"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Border>

                    <Label Text="{ext:Translate DrivingBias}" FontSize="14" FontAttributes="Bold" Margin="0,5,0,0"/>
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:PieChart
                            Series="{Binding DrivingBiasSeries}"
                            LegendPosition="Right"
                            HeightRequest="200" />
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="{ext:Translate ApproachAccuracy}" Style="{StaticResource Title2}" />
                    <Label Text="{ext:Translate ApproachZones}" FontSize="12" TextColor="{StaticResource Gray400}" />
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:CartesianChart
                            Series="{Binding ApproachAccuracySeries}"
                            XAxes="{Binding ApproachXAxes}"
                            HeightRequest="220">
                        </lvc:CartesianChart>
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="{ext:Translate ClubDistances}" Style="{StaticResource Title2}" />
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:CartesianChart
                            Series="{Binding ClubDistanceSeries}"
                            XAxes="{Binding ClubXAxes}"
                            YAxes="{Binding ClubYAxes}"
                            HeightRequest="350">
                        </lvc:CartesianChart>
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="{ext:Translate PuttingPerformance}" Style="{StaticResource Title2}" />
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:CartesianChart
                            Series="{Binding PuttingStatSeries}"
                            XAxes="{Binding PuttingXAxes}"
                            HeightRequest="220">
                        </lvc:CartesianChart>
                    </Border>

                    <Label Text="{ext:Translate Scrambling}" Style="{StaticResource Title2}" Margin="0,10,0,0" />
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:PieChart
                            Series="{Binding ScramblingSeries}"
                            LegendPosition="Right"
                            HeightRequest="200" />
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="{ext:Translate ScoreHistory}" Style="{StaticResource Title2}" />
                    <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
                        <lvc:CartesianChart
                            Series="{Binding ScoreSeries}"
                            XAxes="{Binding XAxes}"
                            YAxes="{Binding YAxes}"
                            HeightRequest="250" />
                    </Border>
                </VerticalStackLayout>

                <BoxView HeightRequest="50" Color="Transparent"/>
            </VerticalStackLayout>
        </ScrollView>

        <Grid IsVisible="{Binding IsBusy}" BackgroundColor="{AppThemeBinding Light=#80FFFFFF, Dark=#80000000}">
            <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}" VerticalOptions="Center" HorizontalOptions="Center"/>
        </Grid>
    </Grid>
</ContentPage>