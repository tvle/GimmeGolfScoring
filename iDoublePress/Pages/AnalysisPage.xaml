<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:iDoublePress.PageModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             xmlns:ext="clr-namespace:iDoublePress.Extensions"
             x:Class="iDoublePress.Pages.AnalysisPage"
             x:DataType="pageModels:AnalysisPageModel"
             x:Name="AnalysisPageView"
             Title="{ext:Translate Analysis}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            BindingContext="{Binding Path=BindingContext, Source={x:Reference AnalysisPageView}, x:DataType=ContentPage}"
            EventName="NavigatedTo"
            Command="{Binding NavigatedToCommand}" />
    </ContentPage.Behaviors>

    <ScrollView>
        <VerticalStackLayout Padding="{StaticResource LayoutPadding}" Spacing="20">

            <Label Text="Score Trends" Style="{StaticResource Title2}" />

            <lvc:CartesianChart
                Series="{Binding ScoreSeries}"
                XAxes="{Binding XAxes}"
                YAxes="{Binding YAxes}"
                HeightRequest="400" />


            <Label Text="Average Scores by Course" Style="{StaticResource Title2}" />

            <lvc:CartesianChart
                Series="{Binding AverageScoreSeries}"
                XAxes="{Binding CourseXAxes}"
                YAxes="{Binding YAxes}"
                HeightRequest="400" />


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>