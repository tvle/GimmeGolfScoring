<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:iDoublePress.PageModels"             
             xmlns:models="clr-namespace:iDoublePress.Models"
             xmlns:controls="clr-namespace:iDoublePress.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:converters="clr-namespace:iDoublePress.Converters"
             xmlns:ext="clr-namespace:iDoublePress.Extensions"
             x:Class="iDoublePress.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage"
             Title="{Binding Today}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedTo"
                Command="{Binding NavigatedToCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedFrom"
                Command="{Binding NavigatedFromCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="Appearing"                
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
            <toolkit:IsNotNullConverter x:Key="IsNotNullConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <pullToRefresh:SfPullToRefresh
            IsRefreshing="{Binding IsRefreshing}"
            RefreshCommand="{Binding RefreshCommand}">
            <pullToRefresh:SfPullToRefresh.PullableContent>
                <ScrollView>
                    <Grid Padding="{StaticResource LayoutPadding}" RowSpacing="{StaticResource LayoutSpacing}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        
                        <!-- Golf Rounds Section -->
                        <Label Grid.Row="2" Text="{ext:Translate Golf}" Style="{StaticResource Title2}" SemanticProperties.HeadingLevel="Level1"/>
                        <VerticalStackLayout Grid.Row="3" Spacing="10" Margin="0,10">
                            <Button Text="{ext:Translate StartNewRoundButton}"
                                    Command="{Binding StartNewRoundCommand}"
                                    BackgroundColor="{StaticResource Primary}"
                                    TextColor="White"
                                    HeightRequest="50"
                                    FontSize="18"
                                    SemanticProperties.Description="{ext:Translate StartNewRoundDescription}"/>
                            
                            <Label Text="{ext:Translate RecentRounds}" Style="{StaticResource Title3}" Margin="0,10,0,0"/>
                            <VerticalStackLayout BindableLayout.ItemsSource="{Binding RecentRounds}"
                                                 Spacing="5">
                                <BindableLayout.ItemTemplate>
                                    <DataTemplate x:DataType="models:Round">
                                        <Border Style="{StaticResource CardStyle}" Margin="0,5">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer 
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type pageModels:MainPageModel}}, Path=NavigateToRoundCommand}"
                                                    CommandParameter="{Binding .}"/>
                                            </Border.GestureRecognizers>
                                            <Grid ColumnDefinitions="*,Auto">
                                                <VerticalStackLayout Grid.Column="0">
                                                    <Label Text="{Binding Course.Name, FallbackValue='Golf Course'}" Style="{StaticResource Title3}"/>
                                                    <Label Text="{Binding StartTime, StringFormat='{0:MMM dd, yyyy}', FallbackValue='Today'}" Style="{StaticResource Caption1}"/>
                                                </VerticalStackLayout>
                                                <VerticalStackLayout Grid.Column="1" HorizontalOptions="End">
                                                    <Label Text="{Binding TotalScore, FallbackValue='0'}" Style="{StaticResource LargeTitle}"/>
                                                    <Label Text="{Binding ScoreDisplay, FallbackValue='E'}" Style="{StaticResource Title2}"/>
                                                </VerticalStackLayout>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                       
                    </Grid>
                </ScrollView>
            </pullToRefresh:SfPullToRefresh.PullableContent>
        </pullToRefresh:SfPullToRefresh>
    </Grid>
</ContentPage>
